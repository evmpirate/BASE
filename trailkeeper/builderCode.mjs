// TrailKeeper's agent Builder Code (ERC-8021), registered via
// POST api.base.dev/v1/agents/builder-codes for the agent's operational wallet
// 0x2C7BDedfC428E8eFe4197325A47f91B82dC33abC (the same burner set as
// setAgentWallet on both ERC-8004 registries). Distinct from DustSweep's app
// code (bc_9a7f6zpz) — agent activity and app activity are attributed separately.
//
// Do NOT re-register: that would mint a new code and orphan this one.
export const BUILDER_CODE = "bc_z2oi30bi";
